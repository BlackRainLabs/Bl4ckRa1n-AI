---\nname: github-code-push\ndescription: GitHub large-file deploy (500+ lines). cp+sed bypasses quoting/limits.\nversion: 1.5.0\nauthor: Black Rain Labs\nlicense: MIT\nmetadata:\n  hermes:\n    tags: [GitHub, Code-Push, Large-Files, Deploy, Skills]\n    related_skills: [github-auth, github-repo-management]\n---\n\n# GitHub Code Pusher\n\nHermes Agent skill for reliable file writes to GitHub repos. Handles 500+ line scripts (PS, bash, Python) bypassing write_file truncation and terminal quoting escapes.\n\n## Prerequisites\n- GitHub auth (`github-auth` skill)\n- Inside cloned repo (`/home/null/bl4ckra1n-repo`)\n\n## Workflow\n\n```\ncd /home/null/bl4ckra1n-repo\nmkdir -p DIR\ncp SOURCE DIR/NAME.ps1\nsed -i 's/^\\\\s*\\\\d+\\\\|//g' DIR/NAME.ps1  # Strip read_file prefixes\ngit add DIR/NAME.ps1  # EXACT filename - NO wildcards!\ngit ls-files DIR/NAME.ps1  # Verify staged\nwc -l DIR/NAME.ps1\ngit commit -m 'Deploy NAME.ps1 (N lines)'\ngit push origin main\ncurl https://raw.githubusercontent.com/BlackRainLabs/Bl4ckRa1n-AI/main/DIR/NAME.ps1 | wc -l\n```\n\n## Quote Escapes FIXED\n**AVOID:** `echo \"content\"` → literal `\\\\n`\n**USE:** \n```\ncat > FILE << 'EOF'\nMulti\nline\ncontent\nEOF\n```\n**OR:** `printf '%s\\n' 'line1' 'line2' > FILE`\n\n## Examples\n- HATUI-v2.ps1 (442 lines): `/HATUI/HATUI-v2.ps1`\n- Skills: `/skills/github-code-push/SKILL.md`\n- bigfinal.txt (600 lines): `/skills/bigfinal.txt`\n\n## Pitfalls\n- **Dir wildcards fail:** `git add DIR/` → pathspec error\n- **Exact filename:** `git add DIR/NAME.ps1`\n- **ls-files verify** staged\n- **GitHub auto-mkdirs** path\n\n**Proven:** 600-line bigfinal.txt → instant raw URL.